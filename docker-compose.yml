services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: denai_murah_app
    volumes:
      - ./denai-murah:/var/www/denai-murah
    networks:
      - app_network

networks:
  app_network:
    external: true
